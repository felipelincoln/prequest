defmodule PrequestWeb.PageLiveTest do
  use PrequestWeb.ConnCase

  # import Phoenix.LiveViewTest

  #  test "disconnected and connected render", %{conn: conn} do
  #    {:ok, page_live, disconnected_html} = live(conn, "/")
  #    assert disconnected_html =~ "homepage"
  #    assert render(page_live) =~ "homepage"
  #  end
end
